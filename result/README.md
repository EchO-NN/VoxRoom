# Result Directory

Runtime outputs are written here by default. This directory is ignored by Git
except for this README.

Typical run layout:

```text
result/<run_name>/
  results.jsonl
  run.log
  roomseg_snapshots/
```

